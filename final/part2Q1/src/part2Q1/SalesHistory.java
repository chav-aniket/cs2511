package part2Q1;

public class SalesHistory {
	
	private Employee salesPerson;

	// This is a lazy class as it has no justified independent functionality
	//	and the method can be placed inside the SalesPerson class with zero effect.
	
	// public String getSalesSummary() {
	// 	return salesPerson.getFirstName() + salesPerson.getLastName() + "Sales Target: " + salesPerson.getSalesTarget() + "$\n" +
	// 		    "Sales to date: " + salesPerson.getSalesAchieved() + "$";
	// }

}
